{
  "name": "census-self-help-oauth",
  "version": "1.0.0",
  "engines": {
    "node": ">=7.x"
  },
  "description": "GitHub OAuth client",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "keywords": [
    "netlify",
    "cms",
    "auth"
  ],
  "license": "MIT",
  "dependencies": {
    "dotenv": "2.0.0",
    "express": "4.17.1",
    "randomstring": "1.1.5",
    "simple-oauth2": "1.2.0"
  }
}
